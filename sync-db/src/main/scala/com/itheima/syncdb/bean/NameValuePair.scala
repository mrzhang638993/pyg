package com.itheima.syncdb.bean

case class NameValuePair(
                        columnName:String,
                        columnValue:String,
                        isValid:Boolean
                        )
