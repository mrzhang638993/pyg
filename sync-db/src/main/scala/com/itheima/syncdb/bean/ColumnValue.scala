package com.itheima.syncdb.bean

case class ColumnValue(
                        columnName:String,
                        columnValue:String,
                        isValid:Boolean
                      )
